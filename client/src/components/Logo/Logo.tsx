import { FC } from 'react';
import { Typography } from 'antd';


const { Text } = Typography;

const Logo: FC = () => {

    return (
        <Text strong>TEAMGEIST</Text>
    )
};

export default Logo;